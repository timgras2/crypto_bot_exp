# =============================================================================
# CRYPTO TRADING BOT CONFIGURATION
# =============================================================================
# ⚠️  IMPORTANT SAFETY NOTICE:
# This bot trades with real money on cryptocurrency exchanges.
# ALWAYS start with very small amounts (€1-5 per trade) for testing!
# Never invest more than you can afford to lose.
# =============================================================================

# =============================================================================
# BITVAVO API CREDENTIALS (REQUIRED)
# =============================================================================
# Get these from your Bitvavo account settings -> API Access
# Enable trading permissions for the bot to place orders
BITVAVO_API_KEY=your_bitvavo_api_key_here
BITVAVO_API_SECRET=your_bitvavo_api_secret_here

# API Configuration (Advanced users only - defaults are usually fine)
BITVAVO_BASE_URL=https://api.bitvavo.com/v2
RATE_LIMIT=300              # API requests per minute (Bitvavo limit: 300)
API_TIMEOUT=30              # API timeout in seconds
OPERATOR_ID=1001            # Required by Bitvavo API for order identification

# =============================================================================
# TRADING PARAMETERS (Optional - safe defaults will be used if not set)
# =============================================================================
# ⚠️ CRITICAL SAFETY SETTINGS - Adjust these carefully!

# Trading amounts
MAX_TRADE_AMOUNT=5.0        # EUR per trade - START VERY SMALL for testing!

# Risk management
MIN_PROFIT_PCT=5.0          # Stop loss percentage (5% loss = sell immediately)
TRAILING_PCT=3.0            # Trailing stop percentage (locks in profits as price rises)

# Bot behavior
CHECK_INTERVAL=10           # Seconds between market checks (10 = every 10 seconds)
MAX_RETRIES=3               # API request retry attempts on failure
RETRY_DELAY=5               # Seconds to wait between retries

# =============================================================================
# FIRST TIME SETUP CHECKLIST
# =============================================================================
# □ 1. Create Bitvavo account and enable API access with trading permissions
# □ 2. Copy your API key and secret to the variables above
# □ 3. Set MAX_TRADE_AMOUNT to a very small amount (€1-5) for testing
# □ 4. Ensure you have sufficient EUR balance in your Bitvavo account
# □ 5. Test with paper trading or very small amounts first
# □ 6. Monitor the bot closely during initial runs
# 
# ⚠️ WARNING: The bot will automatically trade on new cryptocurrency listings.
# Make sure you understand the risks before running in production!